# Application
PORT=3000
NODE_ENV=development
VERBOSE_LOGS=false           # Show NestJS routing logs (true = show, false = hide)
AUTO_SEED=true               # Auto-seed sample data if database is empty (true = enabled)

# PostgreSQL Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=ecommerce
DB_PASSWORD=ecommerce_password
DB_DATABASE=ecommerce_cart_coupon
DB_SYNCHRONIZE=true          # Auto-sync entities to DB (set false in production!)
DB_LOGGING=false             # Show SQL queries in console (true = show, false = hide)

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Redis Lock Settings (for concurrency handling)
REDIS_LOCK_DURATION=10000
REDIS_LOCK_RETRY_COUNT=3
REDIS_LOCK_RETRY_DELAY=200

# NOTE: Rename this file to .env before running the application

